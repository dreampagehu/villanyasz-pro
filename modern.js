const glow=document.createElement('div');glow.className='pointer-glow';document.body.appendChild(glow);let gx=innerWidth/2,gy=innerHeight/2,tx=gx,ty=gy;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY});function follow(){gx+=(tx-gx)*.09;gy+=(ty-gy)*.09;glow.style.transform=`translate(${gx-210}px,${gy-210}px)`;requestAnimationFrame(follow)}follow();
document.querySelectorAll('.service-card,.step').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(800px) rotateX(${-y*4}deg) rotateY(${x*5}deg) translateY(-7px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
addEventListener('scroll',()=>{const y=scrollY;document.querySelector('.hero-visual img').style.translate=`0 ${Math.min(y*.12,70)}px`;document.querySelector('.floating-stat').style.translate=`0 ${Math.min(y*.2,110)}px`},{passive:true});
